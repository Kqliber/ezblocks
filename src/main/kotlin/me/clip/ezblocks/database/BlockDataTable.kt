package me.clip.ezblocks.database
/*

import org.jetbrains.exposed.dao.IntEntity
import org.jetbrains.exposed.dao.id.EntityID
import org.jetbrains.exposed.dao.id.IntIdTable

object BlockDataTable : IntIdTable("data") {

    val uuid = uuid("uuid")
    val blocks = integer("blocks")
}

class BlockDataEntity(id: EntityID<Int>) : IntEntity(id) {

    var uuid by BlockDataTable.uuid
    var blocks by BlockDataTable.blocks
}
*/
